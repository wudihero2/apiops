# ⚠️ 此為範本檔案
#
# 使用方式：
# 1. 複製此檔案為 configmap-secrets.yaml
#    cp configmap-secrets.yaml.example configmap-secrets.yaml
#
# 2. 修改以下密碼為你自己的開發用密碼
#
# 3. 確保 configmap-secrets.yaml 不會被提交到 git
#    (如果你想要更安全，可以將它加入 .gitignore)
#
# ⚠️ 注意：這些密碼僅供本地 kind cluster 使用，絕不可用於生產環境！

apiVersion: v1
kind: ConfigMap
metadata:
  name: apiops-config
  namespace: ops
data:
  ENV: "local"
---
apiVersion: v1
kind: Secret
metadata:
  name: apiops-secret
  namespace: ops
type: Opaque
stringData:
  # 開發用 API Key - 請修改為你自己的值
  api-key: "dev-api-key-CHANGE-ME"

  # 開發用資料庫連線字串 - 請修改密碼
  # 格式: postgresql://username:password@host:port/database
  db-url: "postgresql://apiops:CHANGE-PASSWORD@postgres:5432/apiops"
